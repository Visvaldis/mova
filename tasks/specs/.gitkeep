# Optional scratch space for design notes. Design now happens in plan mode (Seva reviews the plan),
# so a separate spec file is not required — drop one here only if a task explicitly wants to jot
# its design (e.g. thought-lens.md, accent-atlas.md, playground.md).
